server:
  port: 8085

log_level: warn

auth:
  disable_auth: true

session:
  ttl_sec: 3600    # sessions last 1 hour of inactivity
  max_turns: 50    # keep last 50 user+assistant pairs
  # dir: /tmp/ixr-sessions  # uncomment to persist sessions across restarts

providers:
  openai:
    api_key: "${OPENAI_API_KEY}"
  anthropic:
    api_key: "${ANTHROPIC_API_KEY}"
  gemini:
    api_key: "${GOOGLE_API_KEY}"
  gemma:
    api_key: "${GOOGLE_API_KEY}"
  llama:
    api_key: "${GROQ_API_KEY}"
  deepseek:
    api_key: "${DEEPSEEK_API_KEY}"
  cerebras:
    api_key: "${CEREBRAS_API_KEY}"
  mistral:
    api_key: "${MISTRAL_API_KEY}"
  openrouter:
    api_key: "${OPENROUTER_API_KEY}"
  sambanova:
    api_key: "${SAMBANOVA_API_KEY}"
  github:
    api_key: "${GITHUB_TOKEN}"
  zhipu:
    api_key: "${ZHIPU_API_KEY}"

# Note: model chaining ("chains:" config, X-IXR-Chain header) exists only on
# the unmerged feature/model-chain branch, not on main — don't reintroduce a
# chains: block here until that lands, since it silently no-ops otherwise.
